// Re-export everything from theme.ts for backwards compatibility
export {
  Colors,
  Fonts,
  CATEGORY_COLORS,
  DEFAULT_THEME,
  getGradientColor,
  WEEK_SUMMARY_SCORE_STOPS,
  type ThemeColors,
  type ThemeMode,
} from './theme';
