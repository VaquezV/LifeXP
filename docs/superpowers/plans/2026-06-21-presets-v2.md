# Presets V2 — Restructuration & Nouveaux Items

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restructurer les presets Supabase (Soin, Instrument/Solfège, Chien, Traitement, Activité) et améliorer l'affichage des unités et des catégories dans le picker.

**Architecture:** Une migration SQL libère le champ `expertise` (suppression du CHECK constraint) et réorganise les données. `seed.sql` est mis à jour pour refléter l'état final. L'UI `add-habit-modal.tsx` reçoit un formateur d'unités et des badges de catégorie dans le picker.

**Tech Stack:** PostgreSQL (Supabase migrations), React Native / TypeScript

---

## Tableau des changements de presets

| Ancien nom | Ancienne expertise | Nouveau nom | Nouvelle expertise | Type | min | target | max | Catégorie |
|---|---|---|---|---|---|---|---|---|
| Musique | debutant | Instrument | debutant | per_day | 10 | 15 | 30 | dev_perso |
| Musique | intermediaire | Instrument | intermediaire | per_day | 15 | 30 | 60 | dev_perso |
| Musique | expert | Instrument | expert | per_day | 30 | 60 | 90 | dev_perso |
| — | — | Solfège | standard | per_day | 5 | 10 | 20 | dev_perso |
| Chien | standard | Chien | Balade | per_day | 30 | 60 | 90 | vie_familiale |
| — | — | Chien | Brossage quotidien | times_per_day | 0 | 1 | 1 | vie_familiale |
| — | — | Chien | Brossage hebdo | times_per_week | 0 | 1 | 2 | vie_familiale |
| Dents | standard | Soin | Dents | times_per_day | 1 | 2 | 3 | self_care |
| Douche | standard | Soin | Douche | times_per_week | 3 | 5 | 7 | self_care |
| — | — | Soin | Rasage quotidien | times_per_day | 0 | 1 | 1 | self_care |
| — | — | Soin | Rasage bi-hebdo | times_per_week | 0 | 2 | 3 | self_care |
| — | — | Traitement | 1x/jour | times_per_day | 1 | 1 | 3 | self_care |
| — | — | Traitement | 2x/jour | times_per_day | 1 | 2 | 3 | self_care |
| — | — | Traitement | 3x/jour | times_per_day | 2 | 3 | 4 | self_care |
| — | — | Traitement | 1x/semaine | times_per_week | 1 | 1 | 2 | self_care |
| — | — | Activité | Marche | times_per_day | 0 | 1 | 1 | dev_perso |
| — | — | Activité | Courses | times_per_week | 1 | 3 | 5 | dev_perso |
| — | — | Activité | Sport | per_day | 15 | 30 | 60 | dev_perso |

---

## Fichiers touchés

| Action | Fichier | Rôle |
|--------|---------|------|
| Créer | `supabase/migrations/20260621_update_presets_v2.sql` | Migration : libère expertise + restructure données |
| Modifier | `supabase/seed.sql` | Reflect new state pour fresh setups |
| Modifier | `components/add-habit-modal.tsx` | Formateur d'unités + badges catégorie dans picker |

---

## Task 1 : Migration SQL — libération du champ expertise + restructuration

**Fichier à créer :** `supabase/migrations/20260621_update_presets_v2.sql`

- [ ] Créer le fichier avec ce contenu exact :

```sql
-- Migration: Presets V2 — free-form expertise + restructuration

-- 1. Supprimer le CHECK constraint sur expertise pour permettre des valeurs libres
ALTER TABLE public.preset_habits
  DROP CONSTRAINT IF EXISTS preset_habits_expertise_check;

-- 2. Musique → Instrument (renommage du name uniquement, expertise inchangée)
UPDATE public.preset_habits SET name = 'Instrument' WHERE name = 'Musique';

-- 3. Ajouter Solfège
INSERT INTO public.preset_habits
  (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value,
   editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Solfège', 'dev_perso', 'standard', '🎵', 'per_day', 1, 5, 10, 20, true, true, true, false, true);

-- 4. Chien : standard → Balade + ajout Brossage
UPDATE public.preset_habits SET expertise = 'Balade', min_value = 30
  WHERE name = 'Chien' AND expertise = 'standard';

INSERT INTO public.preset_habits
  (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value,
   editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Chien', 'vie_familiale', 'Brossage quotidien', '🐕', 'times_per_day', 1, 0, 1, 1, false, false, false, false, false),
  ('Chien', 'vie_familiale', 'Brossage hebdo',     '🐕', 'times_per_week', 1, 0, 1, 2, false, true,  false, false, false);

-- 5. Dents + Douche → Soin (rename name + rename expertise)
UPDATE public.preset_habits SET name = 'Soin', expertise = 'Dents'
  WHERE name = 'Dents' AND expertise = 'standard';

UPDATE public.preset_habits SET name = 'Soin', expertise = 'Douche', min_value = 3, target_value = 5, max_value = 7
  WHERE name = 'Douche' AND expertise = 'standard';

INSERT INTO public.preset_habits
  (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value,
   editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Soin', 'self_care', 'Rasage quotidien', '🪒', 'times_per_day',  1, 0, 1, 1, false, false, false, false, false),
  ('Soin', 'self_care', 'Rasage bi-hebdo',  '🪒', 'times_per_week', 1, 0, 2, 3, false, true,  false, false, false);

-- 6. Nouveau : Traitement
INSERT INTO public.preset_habits
  (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value,
   editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Traitement', 'self_care', '1x/jour',     '💊', 'times_per_day',  1, 1, 1, 3, false, false, false, false, false),
  ('Traitement', 'self_care', '2x/jour',     '💊', 'times_per_day',  2, 1, 2, 3, false, false, false, false, false),
  ('Traitement', 'self_care', '3x/jour',     '💊', 'times_per_day',  3, 2, 3, 4, false, false, false, false, false),
  ('Traitement', 'self_care', '1x/semaine',  '💊', 'times_per_week', 1, 1, 1, 2, false, false, false, false, false);

-- 7. Nouveau : Activité (dev_perso)
INSERT INTO public.preset_habits
  (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value,
   editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Activité', 'dev_perso', 'Marche',  '🚶', 'times_per_day',  1, 0, 1,  1,  false, false, false, false, false),
  ('Activité', 'dev_perso', 'Courses', '🏃', 'times_per_week', 1, 1, 3,  5,  true,  true,  true,  false, false),
  ('Activité', 'dev_perso', 'Sport',   '⚽', 'per_day',        1, 15, 30, 60, true,  true,  true,  false, false);

-- 8. Badges pour les nouveaux presets
INSERT INTO public.preset_badges (preset_habit_id, badge_level, consecutive_days, badge_name, badge_emoji)
-- Solfège
SELECT id, 1,  1,  'Première session',    '🎵' FROM public.preset_habits WHERE name = 'Solfège'
UNION ALL
SELECT id, 7,  7,  'Une semaine',         '📅' FROM public.preset_habits WHERE name = 'Solfège'
UNION ALL
SELECT id, 30, 30, 'Un mois',             '🎉' FROM public.preset_habits WHERE name = 'Solfège'
UNION ALL
-- Chien Brossage
SELECT id, 1,  1,  'Premier brossage',    '🐕' FROM public.preset_habits WHERE name = 'Chien' AND expertise IN ('Brossage quotidien', 'Brossage hebdo')
UNION ALL
SELECT id, 7,  7,  'Une semaine',         '📅' FROM public.preset_habits WHERE name = 'Chien' AND expertise IN ('Brossage quotidien', 'Brossage hebdo')
UNION ALL
-- Soin (nouveaux items)
SELECT id, 1,  1,  'Premier jour',        '🪒' FROM public.preset_habits WHERE name = 'Soin' AND expertise IN ('Rasage quotidien', 'Rasage bi-hebdo')
UNION ALL
SELECT id, 7,  7,  'Une semaine',         '📅' FROM public.preset_habits WHERE name = 'Soin' AND expertise IN ('Rasage quotidien', 'Rasage bi-hebdo')
UNION ALL
-- Traitement
SELECT id, 1,  1,  'Premier jour',        '💊' FROM public.preset_habits WHERE name = 'Traitement'
UNION ALL
SELECT id, 7,  7,  'Une semaine',         '📅' FROM public.preset_habits WHERE name = 'Traitement'
UNION ALL
SELECT id, 30, 30, 'Un mois',             '🎉' FROM public.preset_habits WHERE name = 'Traitement'
UNION ALL
-- Activité
SELECT id, 1,  1,  'Premier effort',      '⚡' FROM public.preset_habits WHERE name = 'Activité'
UNION ALL
SELECT id, 7,  7,  'Une semaine active',  '📅' FROM public.preset_habits WHERE name = 'Activité'
UNION ALL
SELECT id, 30, 30, 'Un mois actif',       '🎉' FROM public.preset_habits WHERE name = 'Activité';
```

- [ ] Vérifier que le fichier existe : `ls supabase/migrations/20260621_update_presets_v2.sql`

- [ ] Commit : `feat(db): migration presets v2 — Soin, Instrument, Activité, Traitement, Chien`

---

## Task 2 : Mise à jour de seed.sql

**Fichier à modifier :** `supabase/seed.sql`

Ce fichier est utilisé pour les setups Supabase fresh. Il doit refléter l'état **final** désiré (sans les anciens Dents/Douche/Musique/Chien standard).

- [ ] Lire le fichier actuel `supabase/seed.sql` (123 lignes).

- [ ] Remplacer **intégralement** le contenu par la version finale ci-dessous :

```sql
-- Seed data for preset habits — V2
-- Fresh setup: reflects the desired final state after all migrations

-- NOTE: La migration 20260621_update_presets_v2.sql a supprimé le CHECK constraint
-- sur expertise, donc les valeurs libres sont acceptées.

-- Sommeil (inchangé)
INSERT INTO public.preset_habits (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value, editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Sommeil', 'self_care', 'enfant',       '😴', 'per_day', 1, 480, 600, 660, true, true, true, false, false),
  ('Sommeil', 'self_care', 'ado',          '😴', 'per_day', 1, 480, 540, 600, true, true, true, false, false),
  ('Sommeil', 'self_care', 'adulte_homme', '😴', 'per_day', 1, 300, 420, 510, true, true, true, false, false),
  ('Sommeil', 'self_care', 'adulte_femme', '😴', 'per_day', 1, 360, 480, 540, true, true, true, false, false);

-- Soin (ex Dents + Douche, + Rasage)
INSERT INTO public.preset_habits (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value, editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Soin', 'self_care', 'Dents',           '🪥', 'times_per_day',  2, 1, 2, 3, false, true,  true,  false, false),
  ('Soin', 'self_care', 'Douche',          '🚿', 'times_per_week', 1, 3, 5, 7, true,  true,  true,  false, false),
  ('Soin', 'self_care', 'Rasage quotidien','🪒', 'times_per_day',  1, 0, 1, 1, false, false, false, false, false),
  ('Soin', 'self_care', 'Rasage bi-hebdo', '🪒', 'times_per_week', 1, 0, 2, 3, false, true,  false, false, false);

-- Traitement (nouveau)
INSERT INTO public.preset_habits (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value, editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Traitement', 'self_care', '1x/jour',    '💊', 'times_per_day',  1, 1, 1, 3, false, false, false, false, false),
  ('Traitement', 'self_care', '2x/jour',    '💊', 'times_per_day',  2, 1, 2, 3, false, false, false, false, false),
  ('Traitement', 'self_care', '3x/jour',    '💊', 'times_per_day',  3, 2, 3, 4, false, false, false, false, false),
  ('Traitement', 'self_care', '1x/semaine', '💊', 'times_per_week', 1, 1, 1, 2, false, false, false, false, false);

-- Instrument (ex Musique)
INSERT INTO public.preset_habits (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value, editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Instrument',  'dev_perso', 'debutant',      '🎻', 'per_day', 1, 10, 15, 30, true, true, true, false, true),
  ('Instrument',  'dev_perso', 'intermediaire', '🎻', 'per_day', 1, 15, 30, 60, true, true, true, false, true),
  ('Instrument',  'dev_perso', 'expert',        '🎻', 'per_day', 1, 30, 60, 90, true, true, true, false, true);

-- Solfège (nouveau)
INSERT INTO public.preset_habits (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value, editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Solfège', 'dev_perso', 'standard', '🎵', 'per_day', 1, 5, 10, 20, true, true, true, false, true);

-- Activité (nouveau — dev_perso)
INSERT INTO public.preset_habits (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value, editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Activité', 'dev_perso', 'Marche',  '🚶', 'times_per_day',  1, 0,  1,  1,  false, false, false, false, false),
  ('Activité', 'dev_perso', 'Courses', '🏃', 'times_per_week', 1, 1,  3,  5,  true,  true,  true,  false, false),
  ('Activité', 'dev_perso', 'Sport',   '⚽', 'per_day',        1, 15, 30, 60, true,  true,  true,  false, false);

-- Chien (vie_familiale)
INSERT INTO public.preset_habits (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value, editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Chien', 'vie_familiale', 'Balade',           '🐕', 'per_day',        1, 30, 60, 90, true,  true, true,  false, true),
  ('Chien', 'vie_familiale', 'Brossage quotidien','🐕', 'times_per_day',  1, 0,  1,  1,  false, false, false, false, false),
  ('Chien', 'vie_familiale', 'Brossage hebdo',    '🐕', 'times_per_week', 1, 0,  1,  2,  false, true,  false, false, false);

-- Massage Partenaire (inchangé)
INSERT INTO public.preset_habits (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value, editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Massage Partenaire', 'vie_familiale', 'standard', '💆', 'times_per_week', 1, 2, 3, 4, true, true, true, false, true);

-- Enfants (inchangé)
INSERT INTO public.preset_habits (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value, editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Enfants', 'vie_familiale', 'standard', '🧒', 'per_day', 1, 0, 30, 60, true, true, true, false, true);

-- Recettes (inchangé)
INSERT INTO public.preset_habits (name, category, expertise, emoji, frequency_type, frequency_value, min_value, target_value, max_value, editable_min_value, editable_target_value, editable_max_value, editable_frequency_type, editable_frequency_value)
VALUES
  ('Recettes', 'vie_familiale', 'standard', '🍳', 'times_per_week', 1, 3, 5, 8, true, true, true, false, true);

-- ============================================================
-- BADGES
-- ============================================================

-- Badges Sommeil
INSERT INTO public.preset_badges (preset_habit_id, badge_level, consecutive_days, badge_name, badge_emoji)
SELECT id, 1,  1,  'Première nuit complète', '🌙' FROM public.preset_habits WHERE name = 'Sommeil'
UNION ALL
SELECT id, 3,  3,  'Trois nuits',            '🌙🌙🌙' FROM public.preset_habits WHERE name = 'Sommeil'
UNION ALL
SELECT id, 7,  7,  'Une semaine',            '📅' FROM public.preset_habits WHERE name = 'Sommeil'
UNION ALL
SELECT id, 10, 10, 'Dix nuits',              '🎉' FROM public.preset_habits WHERE name = 'Sommeil';

-- Badges Soin
INSERT INTO public.preset_badges (preset_habit_id, badge_level, consecutive_days, badge_name, badge_emoji)
SELECT id, 1,  1,  'Premier jour',  '✨' FROM public.preset_habits WHERE name = 'Soin'
UNION ALL
SELECT id, 7,  7,  'Une semaine',   '📅' FROM public.preset_habits WHERE name = 'Soin'
UNION ALL
SELECT id, 30, 30, 'Un mois',       '🎉' FROM public.preset_habits WHERE name = 'Soin';

-- Badges Traitement
INSERT INTO public.preset_badges (preset_habit_id, badge_level, consecutive_days, badge_name, badge_emoji)
SELECT id, 1,  1,  'Premier jour',  '💊' FROM public.preset_habits WHERE name = 'Traitement'
UNION ALL
SELECT id, 7,  7,  'Une semaine',   '📅' FROM public.preset_habits WHERE name = 'Traitement'
UNION ALL
SELECT id, 30, 30, 'Un mois',       '🎉' FROM public.preset_habits WHERE name = 'Traitement';

-- Badges Instrument
INSERT INTO public.preset_badges (preset_habit_id, badge_level, consecutive_days, badge_name, badge_emoji)
SELECT id, 1,  1,  'Première session',       '🎵' FROM public.preset_habits WHERE name = 'Instrument'
UNION ALL
SELECT id, 7,  7,  'Une semaine de pratique','📅' FROM public.preset_habits WHERE name = 'Instrument'
UNION ALL
SELECT id, 30, 30, 'Un mois de pratique',    '🎉' FROM public.preset_habits WHERE name = 'Instrument';

-- Badges Solfège
INSERT INTO public.preset_badges (preset_habit_id, badge_level, consecutive_days, badge_name, badge_emoji)
SELECT id, 1,  1,  'Première session',  '🎵' FROM public.preset_habits WHERE name = 'Solfège'
UNION ALL
SELECT id, 7,  7,  'Une semaine',       '📅' FROM public.preset_habits WHERE name = 'Solfège'
UNION ALL
SELECT id, 30, 30, 'Un mois',           '🎉' FROM public.preset_habits WHERE name = 'Solfège';

-- Badges Activité
INSERT INTO public.preset_badges (preset_habit_id, badge_level, consecutive_days, badge_name, badge_emoji)
SELECT id, 1,  1,  'Premier effort',     '⚡' FROM public.preset_habits WHERE name = 'Activité'
UNION ALL
SELECT id, 7,  7,  'Une semaine active', '📅' FROM public.preset_habits WHERE name = 'Activité'
UNION ALL
SELECT id, 30, 30, 'Un mois actif',      '🎉' FROM public.preset_habits WHERE name = 'Activité';

-- Badges Chien
INSERT INTO public.preset_badges (preset_habit_id, badge_level, consecutive_days, badge_name, badge_emoji)
SELECT id, 1,  1,  'Premier soin',  '🐕' FROM public.preset_habits WHERE name = 'Chien'
UNION ALL
SELECT id, 7,  7,  'Une semaine',   '📅' FROM public.preset_habits WHERE name = 'Chien'
UNION ALL
SELECT id, 30, 30, 'Un mois',       '🎉' FROM public.preset_habits WHERE name = 'Chien';

-- Badges Massage Partenaire
INSERT INTO public.preset_badges (preset_habit_id, badge_level, consecutive_days, badge_name, badge_emoji)
SELECT id, 1,  1,  'Premier massage',   '💆' FROM public.preset_habits WHERE name = 'Massage Partenaire'
UNION ALL
SELECT id, 7,  7,  'Une semaine',       '📅' FROM public.preset_habits WHERE name = 'Massage Partenaire'
UNION ALL
SELECT id, 30, 30, 'Un mois',           '🎉' FROM public.preset_habits WHERE name = 'Massage Partenaire';

-- Badges Enfants
INSERT INTO public.preset_badges (preset_habit_id, badge_level, consecutive_days, badge_name, badge_emoji)
SELECT id, 1,  1,  'Premier moment',    '🧒' FROM public.preset_habits WHERE name = 'Enfants'
UNION ALL
SELECT id, 7,  7,  'Une semaine',       '📅' FROM public.preset_habits WHERE name = 'Enfants'
UNION ALL
SELECT id, 30, 30, 'Un mois',           '🎉' FROM public.preset_habits WHERE name = 'Enfants';

-- Badges Recettes
INSERT INTO public.preset_badges (preset_habit_id, badge_level, consecutive_days, badge_name, badge_emoji)
SELECT id, 1,  1,  'Première recette',  '🍳' FROM public.preset_habits WHERE name = 'Recettes'
UNION ALL
SELECT id, 7,  7,  'Une semaine',       '📅' FROM public.preset_habits WHERE name = 'Recettes'
UNION ALL
SELECT id, 30, 30, 'Un mois',           '🎉' FROM public.preset_habits WHERE name = 'Recettes';
```

- [ ] Commit : `chore(db): update seed.sql to reflect preset v2 final state`

---

## Task 3 : UI — formateur d'unités + badge catégorie dans le picker

**Fichier à modifier :** `components/add-habit-modal.tsx`

Lis d'abord le fichier entier pour voir l'état actuel (le composant a déjà le picker, les chips niveau, et le formulaire des tâches précédentes).

### Étape A — Formateur d'unités

- [ ] Ajouter cette fonction **avant** la fonction composant `AddHabitModal` (après les constantes `CATEGORY_LABELS`, `FREQ_LABELS`) :

```ts
function formatTarget(preset: PresetHabit): string {
  const v = preset.target_value;
  if (preset.frequency_type === 'per_day') {
    if (v >= 60) {
      const h = Math.floor(v / 60);
      const m = v % 60;
      return m > 0 ? `${h}h ${m}min` : `${h}h`;
    }
    return `${v} min`;
  }
  if (preset.frequency_type === 'times_per_day') return v === 1 ? '1x/jour' : `${v}x/jour`;
  if (preset.frequency_type === 'times_per_week') return `${v}x/sem.`;
  return String(v);
}
```

- [ ] Remplacer dans le JSX de l'étape `level` la ligne :
  ```tsx
  <Text style={[styles.levelChipSub, { color: isDark ? '#888' : '#999' }]}>
    {variant.target_value} min
  </Text>
  ```
  Par :
  ```tsx
  <Text style={[styles.levelChipSub, { color: isDark ? '#888' : '#999' }]}>
    {formatTarget(variant)}
  </Text>
  ```

### Étape B — Badge catégorie dans le picker

- [ ] Ajouter ce mapping **avec** les autres constantes module-level :

```ts
const CATEGORY_SHORT: Record<string, string> = {
  self_care:     'Bien-être',
  dev_perso:     'Dev. perso',
  vie_familiale: 'Famille',
  vie_pro:       'Pro',
};
```

- [ ] Dans le composant, ajouter ce calcul juste après `uniqueNames` :

```ts
const presetByName: Record<string, PresetHabit> = {};
presets.forEach(p => { if (!presetByName[p.name]) presetByName[p.name] = p; });
```

- [ ] Dans le JSX de l'étape `picker`, modifier chaque row pour ajouter le badge. Remplacer le contenu intérieur de la `<Pressable key={name}>` par :

```tsx
<View style={styles.pickerRowInner}>
  <Text style={[styles.presetRowText, { color: isDark ? '#fff' : '#000' }]}>{name}</Text>
  <View style={[styles.categoryBadge, { backgroundColor: CATEGORY_COLORS[presetByName[name]?.category]?.mid ?? '#888' }]}>
    <Text style={styles.categoryBadgeText}>
      {CATEGORY_SHORT[presetByName[name]?.category ?? ''] ?? ''}
    </Text>
  </View>
</View>
<MaterialIcons name="chevron-right" size={20} color={isDark ? '#666' : '#999'} />
```

- [ ] Ajouter dans `StyleSheet.create` :

```ts
pickerRowInner:    { flex: 1, flexDirection: 'row', alignItems: 'center', gap: 8 },
categoryBadge:     { borderRadius: 4, paddingHorizontal: 6, paddingVertical: 2 },
categoryBadgeText: { fontSize: 10, fontWeight: '700', color: '#fff' },
```

- [ ] Vérifier : `npx tsc --noEmit` — 0 erreur.

- [ ] Commit : `feat: preset picker — badge catégorie + formateur d'unités (h/min/x)`

---

## Vérification manuelle

- [ ] Ouvrir le modal → liste des presets : chaque item a un badge coloré de sa catégorie
- [ ] Taper "Sommeil" → chips de niveau avec "7h" et "8h" (non "420 min")
- [ ] Taper "Traitement" → chips "1x/jour", "2x/jour", "3x/jour", "1x/semaine"
- [ ] Taper "Soin" → chips "Dents", "Douche", "Rasage quotidien", "Rasage bi-hebdo"
- [ ] Taper "Chien" → chips "Balade", "Brossage quotidien", "Brossage hebdo"
- [ ] Taper "Instrument" → chips "Débutant (15 min)", "Intermédiaire (30 min)", "Expert (1h)"
- [ ] Créer une habitude depuis un preset → elle apparaît dans la bonne catégorie
